# === Secrets ===
OPENROUTER_API_KEY=sk-or-v1-your-key-here
ZAI_API_KEY=your-zai-api-key-here
TELEGRAM_TOKEN=123456:ABC-your-bot-token-here
BRAVE_API_KEY=your-brave-api-key-here

# === LLM ===
# MODEL examples:
#   glm-4.7                      — Z.AI (cheap, good tool calling)
#   glm-4.5-flash                — Z.AI (cheapest)
#   anthropic/claude-sonnet-4-6  — OpenRouter (best tool calling)
#   google/gemini-2.5-flash      — OpenRouter (cheap, fast)
#   claude-opus-4-local          — Claude Max proxy at localhost:3456 (free with Max subscription)
#   claude-sonnet-4-local        — Claude Max proxy (faster, lighter)
#
# Suffix '-local' routes to Claude Max proxy (localhost:3456).
# Requires: claude-max-api-proxy running locally + Claude Max subscription.
# See README.md for setup instructions.
MODEL=google/gemini-2.5-flash
TEMPERATURE=0.7
MAX_TOKENS=4096

# === Agent ===
MAX_ITERATIONS=15
MEMORY_WINDOW=40

# === Telegram ===
# Comma-separated list of allowed usernames (empty = allow all)
TELEGRAM_ALLOW_FROM=
