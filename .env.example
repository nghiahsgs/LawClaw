# === Secrets ===
TELEGRAM_TOKEN=123456:ABC-your-bot-token-here
BRAVE_API_KEY=your-brave-api-key-here

# === LLM (Claude Max proxy only) ===
# Requires: claude-max-api-proxy running at localhost:3456 + Claude Max subscription.
# See claude-max-api-proxy/ directory and README.md for setup.
#
# Available models:
#   claude-opus-4-local    — Claude Opus 4.6 (best reasoning + tool calling)
#   claude-sonnet-4-local  — Claude Sonnet 4.6 (faster, lighter)
MODEL=claude-opus-4-local
TEMPERATURE=0.7
MAX_TOKENS=4096

# === Agent ===
MAX_ITERATIONS=15
MEMORY_WINDOW=40

# === Telegram ===
# Comma-separated list of allowed usernames (empty = allow all)
TELEGRAM_ALLOW_FROM=
